--- rezound-0.5.1beta/src/frontend_fox/CActionParamDialog.cpp.orig	2002-10-19 08:21:13.000000000 +0200
+++ rezound-0.5.1beta/src/frontend_fox/CActionParamDialog.cpp	2003-05-07 19:33:00.000000000 +0200
@@ -517,7 +517,7 @@
 {
 	CNestedDataFile *presetsFile;
 	FXList *listBox;
-	if(SELID(sel)==ID_NATIVE_PRESET_BUTTON || SELID(sel)==ID_NATIVE_PRESET_LIST)
+	if(FXSELID(sel)==ID_NATIVE_PRESET_BUTTON || FXSELID(sel)==ID_NATIVE_PRESET_LIST)
 	{
 		presetsFile=gSysPresetsFile;
 		listBox=nativePresetList;
--- rezound-0.5.1beta/src/frontend_fox/CMainWindow.cpp.orig	2002-10-21 00:43:29.000000000 +0200
+++ rezound-0.5.1beta/src/frontend_fox/CMainWindow.cpp	2003-05-07 19:34:18.000000000 +0200
@@ -500,7 +500,7 @@
 // file action events
 long CMainWindow::onFileAction(FXObject *sender,FXSelector sel,void *ptr)
 {
-	switch(SELID(sel))
+	switch(FXSELID(sel))
 	{
 	case ID_FILE_NEW_MENUITEM:
 		newSound(gSoundFileManager);
@@ -547,7 +547,7 @@
 // play control events
 long CMainWindow::onPlayControlButton(FXObject *sender,FXSelector sel,void *ptr)
 {
-	switch(SELID(sel))
+	switch(FXSELID(sel))
 	{
 	case ID_PLAY_ALL_ONCE_BUTTON:
 		play(gSoundFileManager,false,false);
@@ -771,17 +771,17 @@
 
 	FXint pos=shuttleDial->getValue();
 
-	if(pos==0 && SELID(sel)==ID_SEEK_LEFT)
+	if(pos==0 && FXSELID(sel)==ID_SEEK_LEFT)
 	{
 		shuttleDial->setValue(pos-inc);
 		onShuttleChange(sender,sel,ptr);
 	}
-	else if(pos==0 && SELID(sel)==ID_SEEK_RIGHT)
+	else if(pos==0 && FXSELID(sel)==ID_SEEK_RIGHT)
 	{
 		shuttleDial->setValue(pos+inc);
 		onShuttleChange(sender,sel,ptr);
 	}
-	else if(pos!=0 && SELID(sel)==ID_SEEK_MODIFY)
+	else if(pos!=0 && FXSELID(sel)==ID_SEEK_MODIFY)
 	{
 		if(pos<0)
 		{ // go more leftward
@@ -800,7 +800,7 @@
 
 long CMainWindow::onViewKey(FXObject *sender,FXSelector sel,void *ptr)
 {
-	switch(SELID(sel))
+	switch(FXSELID(sel))
 	{
 	case ID_CENTER_START_POS:
 		if(gSoundFileManager->getActiveWindow())
@@ -822,14 +822,14 @@
 	CLoadedSound *s=gSoundFileManager->getActive();
 	if(s!=NULL)
 	{
-		if(SELID(sel)==ID_DEFRAG_MENUITEM)
+		if(FXSELID(sel)==ID_DEFRAG_MENUITEM)
 		{
 			s->sound->defragPoolFile();
 			gSoundFileManager->updateAfterEdit();
 		}
-		else if(SELID(sel)==ID_PRINT_SAT_MENUITEM)
+		else if(FXSELID(sel)==ID_PRINT_SAT_MENUITEM)
 			s->sound->printSAT();
-		else if(SELID(sel)==ID_VERIFY_SAT_MENUITEM)
+		else if(FXSELID(sel)==ID_VERIFY_SAT_MENUITEM)
 			s->sound->verifySAT();
 	}
 	else
--- rezound-0.5.1beta/src/frontend_fox/CRecordDialog.cpp.orig	2002-10-19 08:26:16.000000000 +0200
+++ rezound-0.5.1beta/src/frontend_fox/CRecordDialog.cpp	2003-05-07 20:01:33.000000000 +0200
@@ -170,7 +170,7 @@
 		recordingLED->getFirst()->lower();
 
 	// schedule for the next status update
-	timerHandle=getApp()->addTimeout(STATUS_UPDATE_TIME,this,CRecordDialog::ID_STATUS_UPDATE);
+	timerHandle=getApp()->addTimeout(this,CRecordDialog::ID_STATUS_UPDATE,STATUS_UPDATE_TIME);
 	return(1);
 }
 
@@ -195,7 +195,7 @@
 	try
 	{
 		showing=true;
-		timerHandle=getApp()->addTimeout(STATUS_UPDATE_TIME,this,CRecordDialog::ID_STATUS_UPDATE);
+		timerHandle=getApp()->addTimeout(this,CRecordDialog::ID_STATUS_UPDATE,STATUS_UPDATE_TIME);
 
 		// ??? might wanna use rememberShow or some base method
 		if(execute(PLACEMENT_CURSOR) && recorder->getRecordedLength()>0)
@@ -263,10 +263,10 @@
 	try
 	{
 		if(cueNameNumber->getValue()==-1)
-			recorder->addCueNow(cueNamePrefix->getText().text(),SELID(sel)==ID_ADD_ANCHORED_CUE_BUTTON);
+			recorder->addCueNow(cueNamePrefix->getText().text(),FXSELID(sel)==ID_ADD_ANCHORED_CUE_BUTTON);
 		else
 		{
-			recorder->addCueNow((cueNamePrefix->getText().text()+istring(cueNameNumber->getValue())).c_str(),SELID(sel)==ID_ADD_ANCHORED_CUE_BUTTON);
+			recorder->addCueNow((cueNamePrefix->getText().text()+istring(cueNameNumber->getValue())).c_str(),FXSELID(sel)==ID_ADD_ANCHORED_CUE_BUTTON);
 			cueNameNumber->increment();
 		}
 	}
--- rezound-0.5.1beta/src/frontend_fox/CSoundWindow.cpp.orig	2002-09-19 05:49:13.000000000 +0200
+++ rezound-0.5.1beta/src/frontend_fox/CSoundWindow.cpp	2003-05-07 20:04:03.000000000 +0200
@@ -115,7 +115,7 @@
 	CSoundWindow *that=(CSoundWindow *)Pthis;
 	// ??? this is called from another thread.. I don't know if it will cause a problem in FOX
 	if(that->timerHandle==NULL)
-		that->timerHandle=that->getApp()->addTimeout(DRAW_PLAY_POSITION_TIME,that,CSoundWindow::ID_DRAW_PLAY_POSITION);
+		that->timerHandle=that->getApp()->addTimeout(that,CSoundWindow::ID_DRAW_PLAY_POSITION,DRAW_PLAY_POSITION_TIME);
 }
 
 // ----------------------------------------------------------
@@ -535,7 +535,7 @@
 
 long CSoundWindow::onInvertMuteButton(FXObject *sender,FXSelector sel,void *ptr)
 {
-	if(SELTYPE(sel)==SEL_COMMAND)
+	if(FXSELTYPE(sel)==SEL_COMMAND)
 	{
 		for(unsigned t=0;t<loadedSound->sound->getChannelCount();t++)
 			muteButtons[t]->setCheck(!muteButtons[t]->getCheck());
@@ -596,7 +596,7 @@
 		// ??? I could make the calculation of the next event more intelligent
 		// 	- if the onscreen data is smaller I could register to get the timer faster 
 		// reregister to get this event again
-		timerHandle=getApp()->addTimeout(DRAW_PLAY_POSITION_TIME,this,ID_DRAW_PLAY_POSITION);
+		timerHandle=getApp()->addTimeout(this,ID_DRAW_PLAY_POSITION,DRAW_PLAY_POSITION_TIME);
 	}
 	else
 	{
@@ -739,7 +739,7 @@
 		// add the parameters for the dialog to display initially
 		actionParameters.addStringParameter("name","Cue1");
 
-		switch(SELTYPE(sel))
+		switch(FXSELTYPE(sel))
 		{
 		case FXRezWaveView::SEL_ADD_CUE:
 			actionParameters.addSamplePosParameter("position",*((sample_pos_t *)ptr));
--- rezound-0.5.1beta/src/frontend_fox/FXGraphParamValue.cpp.orig	2002-10-23 07:02:55.000000000 +0200
+++ rezound-0.5.1beta/src/frontend_fox/FXGraphParamValue.cpp	2003-05-07 20:09:07.000000000 +0200
@@ -105,7 +105,7 @@
 
 		FXDCWindow dc(this,ev);
 		dc.setForeground(FXRGB(20,20,20));
-		dc.setTextFont(font);
+		dc.setFont(font);
 
 		#define H_TICK_FREQ 50
 
@@ -211,7 +211,7 @@
 
 		FXDCWindow dc(this,ev);
 		dc.setForeground(FXRGB(20,20,20));
-		dc.setTextFont(font);
+		dc.setFont(font);
 
 		#define V_TICK_FREQ 20
 
--- rezound-0.5.1beta/src/frontend_fox/FXRezWaveView.cpp.orig	2002-09-19 05:49:28.000000000 +0200
+++ rezound-0.5.1beta/src/frontend_fox/FXRezWaveView.cpp	2003-05-07 20:10:47.000000000 +0200
@@ -289,7 +289,7 @@
 		e=lastX;
 
 	dc.setForeground(FXRGB(20,20,20));
-	dc.setTextFont(font);
+	dc.setFont(font);
 	for(FXint x=s;x<=e;x++)
 	{
 		if((x%LABEL_TICK_FREQ)==0)
@@ -438,7 +438,7 @@
 
 long FXWaveRuler::onSetPositionToCue(FXObject *object,FXSelector sel,void *ptr)
 {
-	switch(SELID(sel))
+	switch(FXSELID(sel))
 	{
 	case ID_SET_START_POSITION:
 		loadedSound->channel->setStartPosition((sample_pos_t)sound->getCueTime(cueClicked));
@@ -456,7 +456,7 @@
 
 long FXWaveRuler::onAddCue(FXObject *object,FXSelector sel,void *ptr)
 {
-	switch(SELID(sel))
+	switch(FXSELID(sel))
 	{
 	case ID_ADD_CUE:
 		if(parent->target)
--- rezound-0.5.1beta/src/frontend_fox/FXWaveScrollArea.cpp.orig	2002-08-26 07:20:06.000000000 +0200
+++ rezound-0.5.1beta/src/frontend_fox/FXWaveScrollArea.cpp	2003-05-07 20:58:09.000000000 +0200
@@ -299,7 +299,7 @@
 	{
 		if(!(ev->state&SHIFTMASK))
 		{
-			if(startAutoScroll(ev->win_x,ev->win_y))
+			if(startAutoScroll(ev))
 				return 1;
 		}
 	}
